@model IEnumerable<AppMeteo.Models.Pays>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ISO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ISO)
        </td>
        <td>
            @Html.ActionLink("Stations", "Select", "Station", new { id = item.PaysId }, null)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PaysId }) |
            
            @Html.ActionLink("Delete", "Delete", new { id=item.PaysId })
        </td>
    </tr>
}

</table>
